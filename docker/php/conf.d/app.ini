; --- Application PHP settings (dev) ---
memory_limit = 512M
max_execution_time = 60
date.timezone = UTC

; OPcache - safe for dev, revalidates on every request
opcache.enable = 1
opcache.enable_cli = 0
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0

realpath_cache_ttl = 0
